# MyMoney888 个人记账系统

## 项目简介
MyMoney888 是一个功能完备的个人记账系统，支持多账套管理、收支分类、维度管理、统计分析等功能。

## 功能特性

### 核心功能
- **快速记账**：支持支出、收入、转账三种交易类型，可选择成员、商家、标签等维度
- **账户管理**：支持多种账户类型，实时更新余额；支持信用卡、贷款等自动关联
- **账套管理**：支持多账套创建、切换和管理
- **维度管理**：支持成员、商家、标签管理，以及收支分类的二级分类管理
- **统计分析**：支持按时间范围筛选，提供收支分类统计、月度趋势和年度概览
- **用户管理**：支持管理员账户和普通用户，管理员可删除账户
- **投资管理**：支持基金、股票等投资品种管理，自动获取实时行情

### 技术栈
- Vue 3 + Vite + Tailwind CSS
- Vue Router + Pinia
- LocalStorage 数据持久化
- 支持数据库同步（MySQL）

## 版本历史

### v3.6.1
- 修复交易编辑/复制后消失的问题
- 修复账户过滤时类型不匹配导致数据丢失的问题
- 修复信用卡交易余额计算问题
- 修复信用卡管理"查看交易"功能无效问题
- 投资管理收益分析添加时间区间筛选功能
- 统一账户ID类型比较，避免字符串/数字比较问题

### v3.6.0
- 完善信用卡/贷款数据与账户管理的双向同步机制
- 信用卡/贷款数据变更时自动同步到数据库
- 信用卡/贷款/投资账户变更时自动更新账户管理余额
- 账户管理页面监听信用卡、贷款、投资账户更新事件，实时刷新余额

### v3.5.9
- 统一版本号管理，版本号集中配置
- 优化投资管理界面代码查询功能，支持按回车键或失焦自动获取投资信息
- 信用卡/贷款创建时自动同步到账户管理
- 移除数据库全局权限依赖（触发器、存储过程、事件），需在应用层实现
- 清理项目无用文件

### v3.5.8
- 修复 DimensionManagementView.vue 语法错误
- 修复 ledger-templates.js 语法错误
- 移除数据库初始化需要 SUPER 权限的语句

### v3.5.7
- 修复 AccountsView.vue 中 generateId 重复声明问题

### v3.5.5
- 重新设计统计分析中的分类筛选功能，支持二级子分类
- 实现一级和二级分类不能同时选择的逻辑
- 更新投资管理的API，连接真实的数据源获取国内市场数据
- 设置每天9:30、11:30、15:30自动更新持仓产品的最新净值
- 记录投资产品的净值更新日期
- 更新版本号为3.5.5

### v3.5.4
- 增强投资统计展示功能，添加数据可视化图表
- 实现持仓类型分布饼图和账户资产分布环形图
- 添加收益分析柱状图，直观展示每个投资品种的盈亏情况
- 优化收益明细表格，增加持仓占比列
- 集成 Chart.js 库实现数据可视化
- 更新版本号为3.5.4

### v3.5.3
- 增加投资统计展示功能，包括收益查看板块和持仓分析板块
- 添加总投资资产、总收益、总收益率等概览数据
- 实现持仓类型分布和账户资产分布分析
- 添加收益明细表格，显示每个投资品种的详细收益情况
- 更新版本号为3.5.3

### v3.5.2
- 修复统计分析中的分类筛选问题，将分类独立出来并支持二级分类
- 修复投资管理中的API接口问题，实现正确的股票和基金代码识别
- 更新版本号为3.5.2

### v3.5.1
- 修复投资管理页面空白问题
- 确保统计分析功能正确显示二级子分类和空值选项
- 验证投资账户相关的数据库表结构已正确添加
- 更新版本号为3.5.1

### v3.5.0
- 优化统计分析功能：重新设计分类筛选，考虑二级子分类
- 优化统计分析功能：处理成员、商家、标签、支付渠道的空值显示
- 优化统计分析功能：参考随手记、挖财等软件优化界面，添加收支概览卡片、消费洞察等
- 添加投资账户管理功能：创建投资管理相关的组件和页面
- 添加投资账户管理功能：实现投资账户的CRUD操作
- 添加投资账户管理功能：实现投资明细的记录和管理
- 添加投资账户管理功能：集成网上数据库获取实时净值
- 更新版本号为3.5.0

### v3.4.1
- 账户管理界面按类别显示
- 登录网页的图标、名称修改
- 修复新增收支子分类后统计界面不会动态开放选项的问题
- 改进统计分析的筛选功能，添加今年、去年、前年等快捷选项
- 为各个筛选功能添加全选的快捷功能
- 优化统计分析数据显示，根据筛选结果显示，支持饼图、柱状图
- 月度收支趋势跟随上方收支分类选择变动
- 完善信用卡管理功能，添加可操作空间
- 完善贷款管理功能，添加可操作空间
- 完善年度回顾功能，添加可操作空间
- 注册界面添加注册管理员功能
- 更新版本号为3.4.1

### v3.3.0
- 修复安装后界面版本号未更新的问题
- 修复预置支付渠道未显示的问题
- 统一维度管理和统计分析中的术语，将项目改为标签
- 在统计分析界面添加支付渠道筛选
- 修复统计分析中收入分类和支出分类选项缺失的问题
- 修复用户管理界面点击无效的问题
- 移除转账交易中的分类选项
- 在记账界面添加成员、商家、标签选项，在快速记账界面添加支付渠道选项
- 在统计分析界面添加收支分类筛选功能
- 对非管理员用户隐藏【用户管理】导航栏

### v3.2.0
- 修复快速记账功能模块中无法输入成员、商家、标签维度的问题
- 恢复收支分类的二级分类功能，支持完整管理操作
- 在统计分析模块中增加手动选择时间范围的功能，提供常用时间范围选项
- 参考1.9.0版本的设计规范和交互逻辑进行优化

### v3.1.2
- 调整导航栏到左侧边栏显示
- 将记账功能设置为弹窗，在顶部预留按键触发
- 修复账套切换功能
- 修复维度管理功能
- 修复金额计算器功能
- 修复交易记录编辑、复制、删除功能
- 修复管理员账号注册功能
- 增加管理员删除账号的功能

### v3.1.1
- 增加运行日志中显示当前版本
- 重新检查全部功能和之前的bug

### v3.1.0
- 修复登录后需要手动刷新才能看到导航栏的问题
- 恢复账套切换功能
- 恢复维度管理功能
- 恢复金额计算器功能
- 恢复交易记录编辑、复制、删除功能
- 恢复管理员账号注册功能
- 增加管理员删除账号的功能，同步提示是否删除底层数据库相关数据

### v3.0.1
- 修复注册登录后只有记账页面的问题
- 修复导航栏丢失的问题
- 全面检查项目功能完整性

## 安装和运行

### 安装依赖
```bash
npm install
```

### 开发模式运行
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

## 数据存储

### 三重数据持久化机制

系统采用三重数据持久化机制，确保数据安全：

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端浏览器                                │
│                    localStorage (即时缓存)                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        Docker 容器                               │
│  ┌─────────────────┐          ┌─────────────────┐              │
│  │  MyMoney888 App  │ ──────► │   MySQL 8.0     │              │
│  │                 │          │   (关系型存储)   │              │
│  └─────────────────┘          └─────────────────┘              │
│           │                                                      │
│           ▼                                                      │
│  ┌─────────────────┐                                            │
│  │  /data 目录      │ ◄─── Docker 卷挂载                         │
│  │  (JSON文件备份)  │     (持久化到宿主机)                        │
│  └─────────────────┘                                            │
└─────────────────────────────────────────────────────────────────┘
```

### 数据持久化配置

通过 `.env` 文件配置数据路径：

```bash
# 数据持久化配置
DATA_DIR=./data                    # 本地数据备份路径
AUTO_BACKUP_INTERVAL=30            # 自动备份间隔（分钟），0表示禁用
AUTO_BACKUP_ON_CHANGE=true        # 每次同步后自动备份到文件
```

### 备份 API

系统提供以下备份/恢复 API：

| API | 方法 | 说明 |
|-----|------|------|
| `/api/data-dir` | GET | 获取数据目录信息 |
| `/api/backup` | POST | 手动触发全量备份 |
| `/api/backup/:table` | POST | 备份指定表 |
| `/api/restore/:table` | POST | 从文件恢复指定表 |

## Docker 部署

### 方式一：完整部署（推荐）

包含 MySQL 数据库和文件备份：

```bash
# 修改配置
cp .env.example .env
vim .env  # 编辑数据库密码等配置

# 构建并启动
docker-compose up -d

# 查看日志
docker-compose logs -f mymoney888
```

### 方式二：独立部署（仅文件存储）

不依赖 MySQL，数据完全保存在本地文件系统：

```bash
# 使用独立部署配置
docker-compose -f docker-compose.standalone.yml up -d
```

### 方式三：国内网络加速部署

使用国内镜像源加速：

```bash
# 使用国内配置
docker-compose -f docker-compose.cn.yml up -d
```

### 数据卷挂载

Docker 部署会自动创建以下持久化卷：

| 卷名 | 说明 | 挂载路径 |
|------|------|----------|
| `mysql_data` | MySQL 数据文件 | `/var/lib/mysql` |
| `app_data` | 应用备份数据 | `/data` |

### 自定义数据路径

修改 `.env` 文件：

```bash
# MySQL 数据路径
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=your_secure_password

# 应用端口
APP_PORT=8888

# 数据备份路径
DATA_DIR=/your/custom/path
AUTO_BACKUP_INTERVAL=5
```

## 数据恢复

### 从 MySQL 恢复

数据已持久化到 Docker 卷，容器重启后自动恢复。

### 从文件备份恢复

```bash
# 恢复指定表
curl -X POST http://localhost:8888/api/restore/transactions

# 恢复到 MySQL
curl -X POST http://localhost:8888/api/restore/transactions \
  -H "Content-Type: application/json" \
  -d '{"toDb": true}'
```

## 注意事项

- **数据安全**：建议定期检查备份文件 (`./data/*.json`)
- **容器重建**：使用 `docker-compose down -v` 会删除数据卷，如需保留数据请不要加 `-v` 参数
- **版本升级**：升级前建议手动执行 `POST /api/backup` 备份所有数据
